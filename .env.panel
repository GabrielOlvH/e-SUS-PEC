# ========================================
# APS Panel Configuration
# ========================================

# Database Read-Only User (create this user first in PostgreSQL)
PANEL_DB_PASSWORD=your_readonly_password_here

# Panel Admin Credentials
PANEL_ADMIN_USER=admin
# Generate password: openssl rand -base64 12
PANEL_ADMIN_PASSWORD=

# Location Configuration
# Find your IBGE code at: https://www.ibge.gov.br/explica/codigos-dos-municipios.php
CIDADE_IBGE=2927408  # Example: Salvador-BA
ESTADO=BA  # Your state abbreviation
POPULATION=2886698  # Your city's population

# Security Tokens (generate these!)
# Generate SECRET_TOKEN: openssl rand -hex 22
SECRET_TOKEN=

# Generate PASSWORD_SALT: echo '$2a$12$'$(openssl rand -base64 22 | tr -d /=+)
PASSWORD_SALT=

# Panel Port
PANEL_PORT=5001

# Optional: Bridge login URL (leave empty if not using)
BRIDGE_LOGIN_URL=